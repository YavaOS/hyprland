#!/bin/bash

if grep -q "https://stable-mirror.yavaos.org/" /etc/pacman.d/mirrorlist; then
  mirror="stable"
elif grep -q "https://rc-mirror.yavaos.org/" /etc/pacman.d/mirrorlist; then
  mirror="rc"
elif grep -q "https://mirror.yavaos.org/" /etc/pacman.d/mirrorlist; then
  mirror="edge"
else
  mirror="unknown"
fi

if grep -q "https://pkgs.yavaos.org/stable/" /etc/pacman.conf; then
  pkgs="stable"
elif grep -q "https://pkgs.yavaos.org/edge/" /etc/pacman.conf; then
  pkgs="edge"
elif grep -q "https://pkgs.yavaos.org/rc/" /etc/pacman.conf; then
  pkgs="rc"
else
  pkgs="unknown"
fi

if [[ $mirror == $pkgs ]]; then
  echo $mirror
else
  echo "$mirror / $pkgs"
fi
