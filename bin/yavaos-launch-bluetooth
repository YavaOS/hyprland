#!/bin/bash

# Launch the YavaOS bluetooth controls TUI (provided by bluetui).
# Also attempts to unblock bluetooth service if rfkill had blocked it.

rfkill unblock bluetooth
exec yavaos-launch-or-focus-tui bluetui
