#!/bin/bash

# Launch or focus on a given TUI identified by the passed in as the command.
# Use by commands like yavaos-launch-wifi to ensure there is only one wifi configuration screen open.

APP_ID="org.yavaos.$(basename "$1")"
LAUNCH_COMMAND="yavaos-launch-tui $@"

exec yavaos-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
